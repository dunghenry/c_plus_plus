#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
    // Arithmetic operators: +, -, *, %, /, ++, --
    // Assignment Operators: =, +=, -=, *=, %=, /=
    // Comparison Operators: ==, >=, <=, >, <, !=
    // Logical Operators: !, &&, ||
    int a, b;
    cout << "Nhap a: ";
    cin >> a;
    cout << "Nhap b: ";
    cin >> b;
    cout << a << " + " << b << " = " << a + b << endl;
    return 0;
}
